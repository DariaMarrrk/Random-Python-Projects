def get_key(dictionary, value): 
    Keys = []
    for key in dictionary:
        if dictionary[thing] == value:
            Chosen.append(key)
    return(Keys)

""" function gets the key with a specific value from a dictionary.
In case there are multiple keys with the value, the return is a list to store them all
